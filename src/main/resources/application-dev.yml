# Development profile configuration
server:
  port: 8080

gateway:
  logging:
    enabled: true
    # storage: none | file (override as needed)

logging:
  level:
    root: INFO
    me.ryan.acadia: DEBUG
    org.springframework.cloud.gateway: DEBUG
